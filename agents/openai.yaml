interface:
  display_name: "Fortune Teller"
  short_description: "Local-first, auditable Chinese and Western divination"
  default_prompt: "Use $fortune-teller to guide an interactive, local-first reading with explicit calculation facts and uncertainty."
